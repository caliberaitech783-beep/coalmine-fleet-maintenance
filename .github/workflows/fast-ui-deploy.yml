name: Retired - fast-ui-deploy.yml

# Disabled globally at cutover so older branches cannot keep publishing.
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use production-release.yml on azure-hosting-1.0."
